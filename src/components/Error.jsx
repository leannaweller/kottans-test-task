import { h, Component } from 'preact';

class Error extends Component {
  render({repos}) {
      return (
        <div class="repos">
          Error
        </div>
      );
  }
}

export default  Error;
