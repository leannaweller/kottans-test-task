import { h, Component } from 'preact';

class Error extends Component {
  render({repos}) {
      return (
        <div class="repos">
          <h3>Error</h3>
        </div>
      );
  }
}

export default  Error;
