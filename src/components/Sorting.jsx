import { h,  Component } from 'preact';
import './Sorting.less';
class Sorting extends Component {

  render(props,{name}){
    return(
      <div>
        ha ha sorting
      </div>
    );
  }
}

export default Sorting;
